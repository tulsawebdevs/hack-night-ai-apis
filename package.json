{
  "name": "yellownewengineers",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run with-env /bin/bash ./.build.sh",
    "start": "npm run build && npm run with-env node ./.build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "with-env": "npx -y dotenv-cli -e ./.env --"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.16.0"
  }
}
